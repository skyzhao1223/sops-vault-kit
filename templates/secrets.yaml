# 凭据库。敏感字段的值由 sops 加密（白名单见 .sops.yaml），结构与非敏感字段保持明文。
# 用 vault 命令读写，不要手改：
#   vault ls                          列出全部条目（不解密）
#   vault new "工作/示例" --url https://... --username zhangsan
#   vault set "工作/示例" appkey -     从 stdin 写值（pbpaste | …，不进参数/历史）
#   vault shape                       剪贴板入库前体检
#   vault audit                       安全审计
#   vault html                        浏览器卡片视图（用完 vault clean）
systems: {}
